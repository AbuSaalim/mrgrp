import DrivePage from "@/components/drive/DrivePage";

export default function Page() {
  // This is a generic fallback, though users should be routed to their department
  return <DrivePage department="General" />;
}
